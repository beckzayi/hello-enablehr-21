import React from 'react';

const Method = (props) => {
    const value = props.value;

    return (
        <div>
            <h2>Method</h2>
            {value}
        </div>
    );
};

export default Method;
