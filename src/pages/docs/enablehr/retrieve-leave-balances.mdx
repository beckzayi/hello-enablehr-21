---
title: Retrieve Employee Leave Balances
slug: retrieve-leave-balances
---

import YAMLData from '../../../content/enablehr.json';
import Endpoint from '../../../components/endpoint.js';

# Retrieve Leave Balances

<hr />

Some text can go here

<Endpoint
    header={YAMLData}
    child={'get'}
    value={'/accounts/{accountId}/employees/{employeeId}/leave-balances'}
    endpointContext={YAMLData.paths['/accounts/{accountId}/employees/{employeeId}/leave-balances']}
    components={YAMLData.components}
/>
