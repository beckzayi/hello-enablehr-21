---
title: Retrieve Branches
slug: retrieve-branches
---

import YAMLData from '../../../content/enablehr.json';
import Endpoint from '../../../components/endpoint.js';

# Retrieve Branches

<hr />

This is just some static content relating to this endpoint

<Endpoint
    header={YAMLData}
    child={'get'}
    value={'/accounts/{accountId}/branches'}
    endpointContext={YAMLData.paths['/accounts/{accountId}/branches']}
    components={YAMLData.components}
/>
