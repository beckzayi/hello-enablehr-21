---
title: Update Employee
slug: update-employee
---

import YAMLData from '../../../content/enablehr.json';
import Endpoint from '../../../components/endpoint.js';

# Update Employee

<hr />

This should contain something. Not sure what...

<Endpoint
    header={YAMLData}
    child={'put'}
    value={'/accounts/{accountId}/employees/{employeeId}'}
    endpointContext={YAMLData.paths['/accounts/{accountId}/employees/{employeeId}']}
    components={YAMLData.components}
/>
